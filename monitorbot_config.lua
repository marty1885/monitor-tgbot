-- The systemd services that you want to make sure is alive
-- c.monitor.services = {"tor", "sshd"}

-- How long do we wait to test if services are alive?
-- c.monitor.interval = 300

-- How long to wait before the first detection?
-- c.monitor.fireup_delay = 120

-- The Telegram bot token
-- c.bot.token = ""

-- Database path
-- c.bot.db_path = "monitorbot.sqlite"

-- The password used to add user to message list
-- c.bot.password = ""
